# Lessons Learned

## 2026-03-10 — Initial Setup
- Pure stdlib approach for dirty CSV generator keeps it zero-dependency and portable
- Crypto wallet tools need clear security warnings — private keys are dangerous
- Base uses same key format as Ethereum (EVM-compatible), so eth-account works for both
